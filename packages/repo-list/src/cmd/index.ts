import add from './add'
import help from './help'

export const change = (argv: string[]) => argv
export const clone = (argv: string[]) => argv
export const find = (argv: string[]) => argv
export { help,add }